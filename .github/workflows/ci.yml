name: CI

on: [workflow_dispatch, push]

jobs:
  jest:
    name: Jest
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js 20.x (LTS)
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Set up database environment
        run: |
          echo "DATABASE_URL=file:$(pwd)/music-api-test.db\TEST_DATABASE_URL=file:$(pwd)/music-api-test.db" > .env
          cat .env

      - name: Verify Prisma Configuration
        run: npx prisma validate

      - name: Jest Run
        env:
          DATABASE_URL: file:$(pwd)/music-api-test.db
          TEST_DATABASE_URL: file:$(pwd)/music-api-test.db
        run: npm test
